{
    {$CADDY_GLOBAL_OPTIONS}

    frankenphp {
        #worker /path/to/your/worker.php
        {$FRANKENPHP_CONFIG}
    }
}

{$CADDY_EXTRA_CONFIG}

:80 {
    redir https://localhost:443
}

localhost:443 {
    tls {
        # Replace with your certificate path if using a self-signed certificate
        # or let Caddy handle it automatically
        #  https://localhost:443
        #  https://localhost
    }
    root * public/
    encode zstd br gzip

    {$CADDY_SERVER_EXTRA_DIRECTIVES}

    php_server
}
